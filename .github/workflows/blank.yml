# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ env-default ]
  pull_request:
    branches: [ env-default ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  selfhosted-agent_toolsdirectory:
    # The type of runner that the job will run on
    runs-on: self-hosted
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
        env:
          AGENT_TOOLSDIRECTORY: /opt/hostedtoolcache
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version
          which python
          
  selfhosted-runner_tool_cache:
    # The type of runner that the job will run on
    runs-on: self-hosted
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
        env:
          RUNNER_TOOL_CACHE: /opt/hostedtoolcache
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version
          which python
          
  selfhosted:
    # The type of runner that the job will run on
    runs-on: self-hosted
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version
          which python
          
  # This workflow contains a single job called "build"
  hosted-agent_toolsdirectory:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
        env:
          AGENT_TOOLSDIRECTORY: /opt/hostedtoolcache
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version
          which python
          
  hosted-runner_tool_cache:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
        env:
          RUNNER_TOOL_CACHE: /opt/hostedtoolcache
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version
          which python
          
  hosted:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    if: ${{ true }}
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Show env
        run: set
      - name: Setup python
        uses: akv-platform/setup-python@v-sedoli-v2.0.0
        with:
          python-version: 3.7.7
          architecture: x64
      - run: |
          whoami
          ls -l /opt/hostedtoolcache
          python --version     
          which python
